import User from '../models/User.js';
import bcrypt from 'bcryptjs';
import jwt from 'jsonwebtoken';

// This file previously contained a separate loginUser function,
// but it’s now handled directly inside authRoutes.js and has been removed.

// You can use this file later if you want to separate out logic from routes into controllers.

export default {};
